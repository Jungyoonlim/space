import React, { useMemo } from 'react';
import { Line } from '@react-three/drei';
import * as THREE from 'three';

export const TimeSpiral = ({ planet, index }) => {
    const points = useMemo(() => {

        return [];
    }, [planet]);

    return (

    );
};